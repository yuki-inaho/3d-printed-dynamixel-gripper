# 継続作業レビュー — 2026-09-26

## 結論

今回の変更は固定V2の**検証ソフトと追試性の改善**であり、機構の再設計ではない。
旧検証器の誤受理を12負対照で検証すると11件が失敗した。修正後は既存4＋追加48の52件が通過。
保存モデルの441状態と、STEP本体から再抽出した11姿勢を元の閾値で検証した。
製作承認はfalseのまま。旧D6/D7の完了は保存証跡監査の範囲である。

## 発見・修正した問題

|重要度|問題|修正・確認|
|---|---|---|
|高|同じ姿勢11件を全域の姿勢検証と誤認|固定姿勢名・個数・一意性・目標角・入力SHAを検査|
|高|NaNがmax集計をすり抜ける。不正な回転が正規化される|有限値、同次行、直交性、行列式を比較前に検査|
|高|グループの代表変換だけでは部品欠落・誤所属を見逃す|部品数/所属を照合。独立STEP replayで全memberを検査|
|高|python -Oでassertが消える|明示例外へ置換。最適化Pythonの子プロセスでも負対照を拒否|
|中|新しい変換出力を作っても保存モデルを検査する|--modelで選択。別モデルの破損をCLIテストで検出|
|中|joint limits、collision、mesh参照の変更を見逃す|固定config、visual/collision双方、SI scale、path/hashを検査|
|中|過去validation.jsonを暗黙上書き|標準出力を既定とし、--outputは新規ファイルだけ受理|

`step-replay` は全11姿勢を262件ずつ実際に読んだ。局所bounds差は全姿勢0 mm。
旧V1も実STEPを読んだ結果、small_gripper_drive/jaw_lでmember間に5.16416212733822 mmの
相対移動があり、正しく失敗した。単にV1というラベルで拒否したわけではない。
V1の途中出力は `v1-step-negative/failure.json` と失敗時点までのinventoryとして保存した。

## 証跡と実行の区別

元327ファイルは作業前のSHA256を全件照合した。変更前manifestも保持する。
D6はUIログと資料の監査、D7は固定commit/lock・公式release・過去のruntime/wheel/testsの監査。
元counter526の現在の実体はZIP外なので、live値を再確認したとは扱わない。
今回Onshape UI操作と直接Onshape API呼出しはいずれも0。

今回実行したのはPython 3.13.5とCadQuery 2.8.0 / cadquery-ocp 7.9.3.1.1の独立検査である。
元のPixi install、OCCT8再変換、converter wheel再ビルドは未実施。
元CAD18試験に必要なR5 STEPがZIPに存在しないため、その18件も再実行していない。
元13＋4＋18＝35件という数は過去記録。この52件へ足さない。
新しい52件はこの研究の限定suiteであり、リポジトリ全体の試験実行を意味しない。

## 残る制約

材料・造形方向・締結保持・公差・ケーブル・摩擦・連続トルク・疲労/クリープは未知。
カメラ実機の外部校正、反射物の深度性能、把持の実測荷重も未確認。
effort/velocityの0は運動学用の未設定値であり、実機定格ではない。
55面部品を含むcollision meshのwatertight/convex性、質量/慣性、物理エンジンでの動作は保証しない。
既存8接触/小隙間FAIL、完成状態の台座工具アクセスFAIL、720p距離基準FAILを保持する。
441サンプルと11姿勢は連続全関節空間の衝突なし証明ではない。
STEPの局所bounds/面数検査も形状同値性の証明ではない。

今回、新しいOnshapeバージョン、設計候補、印刷、通電、購入、実機試験、GitHubへの書込みは行っていない。
汎用変換器への改善反映と新しいlocked環境構築は、別の実装・検証項目として扱う。

## 続きの優先順位

新しい形状改善より先に、材料・造形方向・把持物と荷重条件を決めた別作業書を作り、
締結保持・連続トルク・公差/ケーブル・実カメラ校正のUNKNOWNを測定で減らす。
OCCT8の完全追試では固定converter commitとlockを取得し、新規出力を--modelで検証する。
この提出に含まれないR5原本やwheelを、存在すると仮定して手順を完了にしない。

## 提出前検査

実tar.zstを作成して別配置へ展開し、全manifest対象ファイルのSHA256を照合した。
そこで52試験、STEP全11姿勢の再読込、441/11照合も再実行した。
再抽出JSONは元の再抽出結果とSHA256まで同一。
整合性検査器は意図的な改変・未登録ファイル追加を正しく拒否し、復旧後に再び合格した。
詳細はpackaging-rehearsal.json。最終提出前には記録更新後のarchiveを再生成し、再展開して照合する。